import { StyleSheet, TextInput } from "react-native";

export default StyleSheet.create({
    textInput:{
        height: 40,
        borderWidth: 1,
        borderColor: 'blue',
        backgroundColor: 'white'
    }
});